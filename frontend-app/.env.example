# Public URL of the main CampusBridge API (no trailing slash).
VITE_API_URL=http://localhost:5000/api

# Point this at your server-side AI proxy or OpenAI-compatible chat endpoint.
# Keep provider API keys on the server, never in the Vite client bundle.
VITE_AI_API_URL=http://localhost:3001/api/ai/chat
